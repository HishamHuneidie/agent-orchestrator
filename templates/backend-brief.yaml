brief_version: "2.0"
feature_id: "{{feature_id}}"
brief_type: backend
objective: "{{objective}}"
context:
  - "{{context}}"
scope:
  - "{{backend_scope_item}}"
out_of_scope:
  - "{{out_of_scope_item}}"
constraints:
  - "{{constraint}}"
risks:
  - "{{risk}}"
dependencies:
  - "{{dependency}}"
involved_files:
  - "{{backend_path}}"
acceptance_criteria:
  - "{{acceptance_criterion}}"
validations:
  - "{{backend_validation_command}}"
definition_of_done:
  - "{{done_condition}}"
known_risks:
  - "{{known_risk}}"
deliverables:
  - "{{deliverable}}"
priority: medium
specialization:
  language: "{{language}}"
  framework: "{{framework}}"
  database: "{{database}}"
  orm: "{{orm}}"
