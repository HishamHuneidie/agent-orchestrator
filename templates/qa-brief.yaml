brief_version: "2.0"
feature_id: "{{feature_id}}"
brief_type: qa
objective: "{{objective}}"
context:
  - "{{context}}"
scope:
  - "{{qa_scope_item}}"
out_of_scope:
  - "{{out_of_scope_item}}"
constraints:
  - "{{constraint}}"
risks:
  - "{{risk}}"
dependencies:
  - "{{dependency}}"
involved_files:
  - "{{path}}"
acceptance_criteria:
  - "{{acceptance_criterion}}"
validations:
  - "{{qa_validation}}"
definition_of_done:
  - "{{done_condition}}"
known_risks:
  - "{{known_risk}}"
deliverables:
  - "{{deliverable}}"
priority: medium
specialization:
  test_layers: ["unit", "e2e", "manual_qa"]
